# Основной фреймворк
Django==4.2.7
argon2-cffi==23.1.0

# REST API
djangorestframework==3.14.0
djangorestframework-simplejwt==5.2.2
django-filter==23.3
django-cors-headers==4.3.1
drf-spectacular==0.27.2

# GraphQL
graphene-django==3.2.3
graphql-relay==3.2.0

# WebSocket / ASGI (Channels подменяет runserver под ASGI в dev без отдельного сервера;
# для продакшена дополнительно установите daphne или uvicorn)
channels==4.0.0
channels-redis==4.1.0

# База данных
psycopg2-binary==2.9.9

# Графовая база данных и алгоритмы
neo4j==5.12.0
networkx==3.1

# Кэш
redis==4.5.5
django-redis==5.4.0

# LLM (разбор текстового описания навыков)
anthropic==0.34.2

# HTTP-клиент для внешних интеграций (GitHub/YouTube/курсы)
requests==2.31.0

# Конфигурация
python-dotenv==1.0.0

# Production-сервер
gunicorn==21.2.0
uvicorn[standard]==0.27.0

# Celery
celery[redis]==5.3.6
flower==2.0.1

# Error tracking
sentry-sdk[django,celery]==2.19.2

# Monitoring (Prometheus metrics at /metrics/)
django-prometheus==2.3.1

# Structured JSON logging (production)
python-json-logger==2.0.7
